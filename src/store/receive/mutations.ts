import { MutationTree } from 'vuex';
import { ReceiveStateInterface } from './state';

const mutation: MutationTree<ReceiveStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
};

export default mutation;
